boot_1 <- boot_2 <- boot_difference <- boot_fit <- boot_index_difference <- d <- d_boot <-
d_boot_high <- d_boot_low <- d_high <- d_low <- d_random <- d_random_high <-
d_random_low <- difference <- estimate <- fit_index <- free.parameter <- group <-
intercept <- invariant <- lv <- model <- model_difference <- model_number <- n_boot <-
n_random <- non_invariant <- op <- random_1 <- random_2 <- random_difference <-
random_fit <- random_index_difference <- random_non_invariant <- slope <- std.all <-
std.error <- term <- type <- x <- y <- ymax <- ymin <- NULL

library(ggplot2)
library(lavaan)
library(broom)
library(cowplot)
library(dplyr)
library(tidyr)
library(introdataviz)
